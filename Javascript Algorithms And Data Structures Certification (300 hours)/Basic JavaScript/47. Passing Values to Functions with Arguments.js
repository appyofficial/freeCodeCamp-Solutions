/*
1. Create a function called functionWithArgsthat accepts two arguments and outputs their sum to the dev console.

2. Call the function with two numbers as arguments
 */

function functionWithArgs(x, y) {
    var sum = x + y;
    console.log(sum);
}

functionWithArgs(100, 50);